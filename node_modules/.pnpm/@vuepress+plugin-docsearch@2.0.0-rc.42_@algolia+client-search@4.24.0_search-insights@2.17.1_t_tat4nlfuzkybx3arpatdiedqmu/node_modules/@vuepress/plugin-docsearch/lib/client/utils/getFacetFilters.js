/**
 * Get facet filters for current lang
 */
export const getFacetFilters = (lang, rawFacetFilters = []) => [
    `lang:${lang}`,
    ...(Array.isArray(rawFacetFilters)
        ? rawFacetFilters
        : [rawFacetFilters]),
];
